una clase cuenta con atributo titular y cantidad(float), el titutlar es un dato obligatorio y cantidad opcional, crea 2 constructores, metodos get,set y tostring y otros 2 metodos ingresar y retirar que reciben una cantidad positiva y no deja retirar si la cantidad esta por debajo de 0

clase persona atributos nombre, edad , dni,sexo,peso y altura.todos menos el dni tendran valores por defecto(valores 0 y sexo hombre) , crea un contructor para ello, otro que de nombre edad sexo y el resto por defecto, otro constructor con todos los atributos como parametros. metodo calcular indice de masacorp(peso en kg/altura^2 en metros) si devuelve<20 la funcion devuelve -1, si devuelve entre 20 y 25 incluidos esta por debajo devuelve 0, por encima 1. Funcion esmayordeedad(si es +18),funcion comprobar sexo recibe un char sexo y devuelve bool,funcion string metodos set de cada parametro con set get, funcion generar dni(generra numero aleatorio de 8 cifras)